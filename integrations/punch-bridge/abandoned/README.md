# Not part of the working setup

Everything in this folder belongs to two approaches that were tried and
abandoned before eTimeTrackLite's own `eTimeTrackLite1.mdb` was found. It is
kept as a record of what was ruled out and why — see the bottom of
`../README.md`.

**Nothing here needs to run, be fixed, or be found.** An AI tool sent looking
for `diagnose.cmd`, `schema.sql` or `schema.mysql.sql` has been handed the
wrong section of the documentation; the task is `docs/ANTIGRAVITY_OFFICE_PC.md`.

The working setup is two files, one folder up:

- `../ettl-sync.ps1`
- `../ettl-employee-map.ps1`
